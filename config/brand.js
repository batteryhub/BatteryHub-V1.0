/* --------------------------- /config/brand -------------------------- */
const BRAND = Object.freeze({
  name: 'BatteryHub',
  // Drop official SVG/PNG exports here; the Logo component switches to them automatically.
  logoSrc: null, logoCompactSrc: null, logoIconSrc: null,
});
