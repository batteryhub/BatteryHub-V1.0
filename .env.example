# Public, client-side settings (currently set in src/config/env.js). Values intentionally left blank.
PUBLIC_PROVIDER=
PUBLIC_API_BASE_URL=
PUBLIC_DEFAULT_LOCALE=
PUBLIC_DEFAULT_STATION=

# Server-only settings belong to the BatteryHub backend, never this client.
# Listed here only so the backend team knows they exist. Names are placeholders.
# SERVER_ONLY_PAYMENT_SECRET_KEY=
# SERVER_ONLY_PAYMENT_WEBHOOK_SECRET=
# SERVER_ONLY_WDIAN_API_KEY=
